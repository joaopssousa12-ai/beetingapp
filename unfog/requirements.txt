fastapi==0.115.0
uvicorn==0.32.0
jinja2==3.1.4
python-multipart==0.0.12
itsdangerous==2.2.0
anthropic>=0.116.0
psycopg2-binary==2.9.10
# Notifications are optional. To enable web push, also install
# requirements-push.txt and set the VAPID_* env vars (see docs/NOTIFICATIONS.md).
# Kept out of the core build so a push-lib compile issue can never break deploys.
